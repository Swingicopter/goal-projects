// Given the array ["cat", "elephant", "dog"], return an array with the first letter of each string.

let arr = ["cat", "elephant", "dog"]

let newArr = arr.map((a) => {return a[0]})

console.log(newArr)